apiVersion: v1
kind: Service
metadata:
  name: {{ include "qcloud-exporter.fullname" . }}
  labels:
    {{- include "qcloud-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qcloud-exporter.selectorLabels" . | nindent 4 }}
