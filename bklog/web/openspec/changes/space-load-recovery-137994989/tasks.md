# Tasks

- [ ] Add reactive unresolved-space bootstrap state and inline homepage authorization handling.
- [ ] Keep the space selector usable and preserve original IndexId specifically during failed-space recovery.
- [ ] Reuse the existing index adaptation path; verify normal and scene switching behavior remains intact.
- [ ] Add focused regression coverage and execute lint, impact analysis, and bundled UI case generation.
- [ ] Resolve the UI execution gate and record actual self-test outcomes.
- [ ] Review diff, commit with --story=137994989, create PR using bundled AAFE CLI, and backfill TAPD with idempotency marker.
