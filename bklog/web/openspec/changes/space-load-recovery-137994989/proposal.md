# Space load recovery

## Why
TAPD 137994989 要求首页空间解析失败后仍能切换空间恢复检索。当前 main.js 跳转 un-authorized，且空间切换会清除路径 IndexId，导致原链接指定索引无法参与恢复适配。

## What Changes
- 首页空间解析失败时在原页面渲染未授权提示，保留路径和 query。
- 空间选择器在未匹配当前空间时仍能展开并加载可选空间。
- 失败恢复切换保留原 IndexId，重新执行现有索引适配。
- 正常空间切换、索引适配优先级和非首页授权行为保持原有语义。
