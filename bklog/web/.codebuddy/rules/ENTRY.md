# CodeBuddy Rules Entry

> **AI 指令**: 本项目的 Rules 存放在公共目录，请读取以下路径获取完整配置。

## Rules 路径

请读取 `../../.ai-shared/rules/` 目录下的所有规则文件：

- `../../.ai-shared/rules/vue-development-mode.mdc` - Vue 开发模式约束
# CodeBuddy Rules Entry

> **AI 指令**: 本项目的 Rules 存放在公共目录，请读取以下路径获取完整配置。

## Rules 路径

请读取 `../../.ai-shared/rules/` 目录下的所有规则文件：

- `../../.ai-shared/rules/vue-development-mode.mdc` - Vue 开发模式约束
