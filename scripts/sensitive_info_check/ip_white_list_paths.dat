# Allowlist file/path patterns for IP check.
#
# Lines are treated as grep patterns and will be matched against output lines:
#   <file>:<line>:<match>
#
# Typical usage: ignore generated lock files that may contain dotted version strings
# which look like IPv4 (e.g. 2.9.21.202).

bkmonitor/uv.lock


# Test fixture expectations contain dummy IPs (1.1.1.1 / 2.2.2.2) defined by the
# shared add_bcs_nodes fixture in conftest; they are long-public placeholder data.
bkmonitor/packages/monitor_web/tests/scene_view/bkmonitor/models/test_bcs_node.py
